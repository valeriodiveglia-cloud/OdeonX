"const fs = require('fs');\nconst path = require('path');\n\nconst projectDir = '/Users/valerio/Documents/Developement Apps/foodcost-webapp';\nconst financeDir = path.join(projectDir, 'src/app/finance');\nconst i18nFile = path.join(projectDir, 'src/lib/i18n.ts');\n\n// 1. Read i18n.ts to find all defined keys\nconst i18nContent = fs.readFileSync(i18nFile, 'utf8');\n\n// The keys are inside const dict = { en: { ... } }\n// Let's find keys under 'en:' block\nconst enBlockMatch = i18nContent.match(/en:\\s*\\{([\\s\\S]*?)\\},\\s*\\n\\s*vi:/);\nif (!enBlockMatch) {\n    console.error('Could not find en: block in i18n.ts');\n    process.exit(1);\n}\nconst enBlock = enBlockMatch[1];\nconst definedKeys = new Set();\n// Simple regex to match keys\nconst keyRegex = /^\\s*([A-Za-z0-9_]+)\\s*:/gm;\nlet match;\nwhile ((match = keyRegex.exec(enBlock)) !== null) {\n    definedKeys.add(match[1]);\n}\n\nconsole.log(`Found ${definedKeys.size} defined keys in i18n.ts`);\n\n// 2. Recursively find all .tsx and .ts files in finance directory\nfunction getFiles(dir) {\n    let results = [];\n    const list = fs.readdirSync(dir);\n    list.forEach(file => {\n        const fullPath = path.join(dir, file);\n        const stat = fs.statSync(fullPath);\n        if (stat && stat.isDirectory()) {\n            results = results.concat(getFiles(fullPath));\n        } else if (file.endsWith('.tsx') || file.endsWith('.ts')) {\n            results.push(fullPath);\n        }\n    });\n    return results;\n}\n\nconst files = getFiles(financeDir);\nconsole.log(`Found ${files.length} ts/tsx files in src/app/finance`);\n\n// 3. Scan files for t(..., 'KEY') and t(..., \"KEY\")\nconst missingKeys = {};\nconst usedKeys = {};\n\n// Regex for t(language, 'KEY') or t('KEY', language)\n// Let's look for t(..., '...') or t(..., \"...\")\nconst tCallRegex = /t\\(\\s*([^,)]+)\\s*,\\s*['\"]([^'\"]+)['\"]\\s*\\)|t\\(\\s*['\"]([^'\"]+)['\"]\\s*,\\s*([^)]+)\\s*\\)/g;\n\nfiles.forEach(file => {\n    const content = fs.readFileSync(file, 'utf8');\n    let m;\n    tCal
<truncated 1859 bytes>