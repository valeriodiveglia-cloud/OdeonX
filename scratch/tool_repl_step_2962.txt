"### 1. Dizionario di Traduzione (`src/lib/i18n.ts`)\n- **Dettagli**: \n  - Aggiunte tutte le chiavi necessarie per tradurre il modulo Finance (dashboard, chart of accounts, impostazioni e navigazione laterale).\n  - Risolti errori di compilazione TypeScript eliminando le chiavi duplicate (`Accounts`, `Code`, `Status`, `Active`) che facevano fallire la build di produzione.\n  - Creati prefissi specifici (es. `FinanceActionCenter`, `FinanceItemsRequiringAttention`, `FinanceAllCaughtUp`, `FinanceNoPendingActions`, `FinanceReview`) per prevenire collisioni di traduzione con altre sezioni dell'app (es. CRM/HR).\n  - Allineata la traduzione vietnamita di `Period` a `'Kỳ'` (precedentemente `'Kỳ hạn'`), rendendola corretta sia per la dashboard finanziaria che per la sezione dei payout.\n\n### 2. Gestione Stato Lingua Locale vs Database (`src/contexts/SettingsContext.tsx`)\n- **Dettagli**:\n  - Risolto un bug architetturale che impediva il cambio lingua per i ruoli non amministrativi (come `accountant` o `staff`).\n  - Originariamente, la funzione di inizializzazione e sincronizzazione del contesto (`hydrateFromSources`) sovrascriveva sempre la scelta locale (`localStorage`) del client con quella globale del database (`app_settings`), che per l'accountant è di sola lettura per via delle policy RLS (Row-Level Security) e quindi bloccata su inglese (`'en'`).\n  - Modificato il contesto affinché rispetti e dia precedenza alla selezione della lingua memorizzata in `localStorage`, consentendo a ciascun utente di cambiare la propria lingua di sessione indipendentemente dalle restrizioni del database globale."