"const fs = require('fs');\nconst path = require('path');\n\nconst projectDir = '/Users/valerio/Documents/Developement Apps/foodcost-webapp';\nconst i18nFile = path.join(projectDir, 'src/lib/i18n.ts');\nconst tempFile = path.join(__dirname, 'temp_i18n.js');\n\nconst content = fs.readFileSync(i18nFile, 'utf8');\n\n// Strip TypeScript annotations and export the dict object\nconst cleanContent = content\n    .replace(/export type Lang = [^;]+;?/g, '')\n    .replace(/export const t = [\\s\\S]+$/g, '') // remove function t\n    + '\\nmodule.exports = dict;';\n\nfs.writeFileSync(tempFile, cleanContent, 'utf8');\n\nlet dict;\ntry {\n    dict = require(tempFile);\n} catch (err) {\n    console.error('Failed to require temp_i18n.js:', err);\n    if (fs.existsSync(tempFile)) fs.unlinkSync(tempFile);\n    process.exit(1);\n}\n\n// Cleanup temp file\nif (fs.existsSync(tempFile)) fs.unlinkSync(tempFile);\n\nconst en = dict.en;\nconst vi = dict.vi;\n\nconst enKeys = Object.keys(en);\nconst viKeys = Object.keys(vi);\n\nconsole.log(`English keys: ${enKeys.length}`);\nconsole.log(`Vietnamese keys: ${viKeys.length}`);\n\n// 1. Missing keys in vi\nconst missingInVi = enKeys.filter(k => !(k in vi));\nconsole.log(`\\n=== Keys in 'en' but missing in 'vi' (${missingInVi.length}) ===`);\nif (missingInVi.length > 0) {\n    missingInVi.forEach(k => console.log(`  - ${k}: \"${en[k]}\"`));\n} else {\n    console.log('None.');\n}\n\n// 2. Keys that have the exact same value in both (excluding numbers, abbreviations, common identical terms)\nconst sameValue = enKeys.filter(k => {\n    if (!(k in vi)) return false;\n    const valEn = String(en[k]).trim();\n    const valVi = String(vi[k]).trim();\n    \n    if (!valEn || !valVi) return false;\n    if (valEn === valVi) {\n        if (/^[A-Z0-9_\\-\\s.]+$/.test(valEn) && valEn.length <= 4) return false;\n        if (['checking', 'saving', 'cash', 'email', 'card', 'usd', 'vnd', 'mpos', 'coa', 'ok', 'pdf', 'excel', 'csv', 'google workspace', 'google', 'card (mpos)'].includes(valEn.toLowerCase())) re
<truncated 750 bytes>