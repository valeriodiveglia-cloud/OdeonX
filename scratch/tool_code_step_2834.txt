"const fs = require('fs');\n\nconst content = fs.readFileSync('/Users/valerio/Documents/Developement Apps/foodcost-webapp/src/lib/i18n.ts', 'utf8');\n\n// We want to find keys inside dict.en and dict.vi\n// We can do a simple parsing by matching key: value lines\nfunction findDuplicates(sectionName, startStr, endStr) {\n  const startIndex = content.indexOf(startStr);\n  if (startIndex === -1) {\n    console.log(`Could not find start of ${sectionName}`);\n    return;\n  }\n  const endIndex = content.indexOf(endStr, startIndex);\n  if (endIndex === -1) {\n    console.log(`Could not find end of ${sectionName}`);\n    return;\n  }\n\n  const section = content.substring(startIndex, endIndex);\n  const lines = section.split('\\n');\n  const seen = new Set();\n  const duplicates = [];\n\n  for (const line of lines) {\n    // Match something like \"    Key: 'Value',\" or \"    Key: `Value`,\" or \"    Key: \\\"Value\\\",\"\n    const match = line.match(/^\\s*([a-zA-Z0-9_]+)\\s*:/);\n    if (match) {\n      const key = match[1];\n      if (seen.has(key)) {\n        duplicates.push(key);\n      } else {\n        seen.add(key);\n      }\n    }\n  }\n\n  console.log(`${sectionName} Duplicates:`, duplicates);\n}\n\nfindDuplicates('English (en)', '  en: {', '  vi: {');\nfindDuplicates('Vietnamese (vi)', '  vi: {', '} as const');\n"