"const fs = require('fs');\nconst path = require('path');\n\nconst projectDir = '/Users/valerio/Documents/Developement Apps/foodcost-webapp';\nconst financeDir = path.join(projectDir, 'src/app/finance');\nconst i18nFile = path.join(projectDir, 'src/lib/i18n.ts');\n\n// 1. Read i18n.ts and parse defined keys\nconst i18nContent = fs.readFileSync(i18nFile, 'utf8');\n\n// Find all lines inside dict.en structure.\n// We can find all keys that are defined as \"KeyName: 'value'\" or \"KeyName: `value`\" or \"KeyName: \\\"value\\\"\"\n// inside dict = { en: { ... }, vi: { ... } }.\n// Let's extract the \"en: {\" to \"},\" block first.\nconst enBlockStart = i18nContent.indexOf('en: {');\nif (enBlockStart === -1) {\n    console.error('Could not find \"en: {\" in i18n.ts');\n    process.exit(1);\n}\n\n// Simple brace matching to extract the en block\nlet braceCount = 1;\nlet i = enBlockStart + 5;\nwhile (braceCount > 0 && i < i18nContent.length) {\n    if (i18nContent[i] === '{') braceCount++;\n    else if (i18nContent[i] === '}') braceCount--;\n    i++;\n}\nconst enBlock = i18nContent.substring(enBlockStart + 5, i - 1);\n\n// Find keys in the block\nconst definedKeys = new Set();\nconst lines = enBlock.split('\\n');\nlines.forEach(line => {\n    // Look for patterns like: KeyName: \"value\" or KeyName: 'value' or KeyName: `value`\n    const match = line.match(/^\\s*([a-zA-Z0-9_]+)\\s*:/);\n    if (match) {\n        definedKeys.add(match[1]);\n    }\n});\n\nconsole.log(`Parsed ${definedKeys.size} keys from i18n.ts (en block)`);\n\n// 2. Recursively find tsx/ts files\nfunction getFiles(dir) {\n    let results = [];\n    const list = fs.readdirSync(dir);\n    list.forEach(file => {\n        const fullPath = path.join(dir, file);\n        const stat = fs.statSync(fullPath);\n        if (stat && stat.isDirectory()) {\n            results = results.concat(getFiles(fullPath));\n        } else if (file.endsWith('.tsx') || file.endsWith('.ts')) {\n            results.push(fullPath);\n        }\n    });\n    return results;\n}\n\nco
<truncated 2439 bytes>