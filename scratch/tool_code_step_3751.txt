"const fs = require('fs');\nconst path = require('path');\n\nconst projectDir = '/Users/valerio/Documents/Developement Apps/foodcost-webapp';\nconst i18nFile = path.join(projectDir, 'src/lib/i18n.ts');\n\nconst content = fs.readFileSync(i18nFile, 'utf8');\n\n// We want to extract the en block and the vi block.\n// A simple way is to use regex or eval.\n// Let's clean the TS syntax and evaluate the dict object.\n// Remove types and exports:\nlet jsContent = content\n    .replace(/export type Lang = [^;]+;?/g, '')\n    .replace(/export const t = [\\s\\S]+$/g, '') // remove function t\n    .replace(/const dict =/g, 'global.dict =');\n\ntry {\n    eval(jsContent);\n} catch (err) {\n    console.error('Failed to parse i18n.ts content:', err);\n    process.exit(1);\n}\n\nconst dict = global.dict;\nif (!dict) {\n    console.error('dict object not found after evaluation');\n    process.exit(1);\n}\n\nconst en = dict.en;\nconst vi = dict.vi;\n\nconst enKeys = Object.keys(en);\nconst viKeys = Object.keys(vi);\n\nconsole.log(`English keys: ${enKeys.length}`);\nconsole.log(`Vietnamese keys: ${viKeys.length}`);\n\n// 1. Missing keys in vi\nconst missingInVi = enKeys.filter(k => !(k in vi));\nconsole.log(`\\n=== Keys in 'en' but missing in 'vi' (${missingInVi.length}) ===`);\nif (missingInVi.length > 0) {\n    missingInVi.forEach(k => console.log(`  - ${k}: \"${en[k]}\"`));\n} else {\n    console.log('None.');\n}\n\n// 2. Keys that have the exact same value in both (excluding numbers, symbols, abbreviations, empty strings, and keys we expect to be identical e.g. \"VND\", \"USD\")\nconst sameValue = enKeys.filter(k => {\n    if (!(k in vi)) return false;\n    const valEn = String(en[k]).trim();\n    const valVi = String(vi[k]).trim();\n    \n    // Ignore empty/special\n    if (!valEn || !valVi) return false;\n    if (valEn === valVi) {\n        // Ignore currency codes or short abbreviations or single words that are identical in both languages (e.g. \"VAT\", \"OK\", \"CSV\", \"Email\", \"MPOS\", \"Google Workspace\", \"Checking
<truncated 1159 bytes>